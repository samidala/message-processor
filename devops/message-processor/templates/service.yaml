apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-processor.fullname" . }}
  labels:
    app: {{ include "message-processor.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "message-processor.name" . }}
